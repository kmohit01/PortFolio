const Divider = () => {
  return (
    <div style={styles.divider}></div>
  );
};

const styles = {
  divider: {
    width: '100%',
    height: '2px', // Adjust this based on the height you need
    backgroundColor: 'white',
  },
};

export default Divider;
